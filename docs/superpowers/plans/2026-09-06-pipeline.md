# Ligar o tubo — Plano

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ligar captura → classificação → normalização → índice no content script, e provar o caminho inteiro na Biblioteca real.

**Architecture:** Uma função pura recebe uma captura e um store e devolve quantos anúncios entraram. O content script vira apenas a cola entre a mensagem e essa função — assim o tubo é testável sem navegador, e o e2e prova só a ligação.

**Tech Stack:** TypeScript 7, Vitest 5, Playwright 1.63.

**Spec:** `docs/superpowers/specs/2026-09-05-copyhaunt-ads-design.md` (seção 6)

## Por que este plano existe

Todas as peças estão prontas e testadas em isolamento, e **nenhuma está
ligada**. O `content/index.ts` importa uma única coisa: `isCopyHauntMessage`.

```
captura → classificar → normalizar → store → critérios → DOM
   ok         ok            ok         ok        ok       falta
```

Cada peça tem teste; o caminho inteiro não tem nenhum. É o caso clássico de
unidades verdes com sistema inerte.

## Global Constraints

- **Permissões continuam exatamente `["storage"]`** e um único host.
- **Nenhuma requisição à Meta.**
- **npm no PowerShell do Windows:** usar `npm.cmd` e `npx.cmd`.
- **Quem commita é o revisor.** O executor escreve `.commit-msg` na raiz e para.
- **Commits:** padrão de `CLAUDE.md`, tipo em inglês, texto em pt-BR.

---

## Task 1: O tubo, como função pura

**Files:**
- Create: `src/content/pipeline.ts`
- Test: `tests/pipeline.test.ts`

**Interfaces:**
- Consumes: `classificar` de `src/core/router.ts`; `normalizarBusca` de
  `src/core/normalize.ts`; `AdStore` de `src/core/store.ts`; `Captura` de
  `src/interceptor/xhr-patch.ts`.
- Produces: de `src/content/pipeline.ts` —
  `interface ResultadoCaptura { tipo: string; novos: number; total: number }`
  e `processarCaptura(captura: Captura, store: AdStore): ResultadoCaptura`.

- [ ] **Step 1: Escrever o teste que falha**

Criar `tests/pipeline.test.ts`:

```ts
import { readFileSync } from 'node:fs'
import { join, resolve } from 'node:path'
import { describe, expect, it } from 'vitest'
import { processarCaptura } from '../src/content/pipeline'
import { AdStore } from '../src/core/store'

const PASTA = resolve(import.meta.dirname, 'fixtures')

function captura(arquivo: string) {
  return {
    url: 'https://www.facebook.com/api/graphql/',
    corpo: readFileSync(join(PASTA, arquivo), 'utf8'),
  }
}

describe('processarCaptura', () => {
  it('indexa os anúncios de uma resposta de busca real', () => {
    const store = new AdStore()
    const r = processarCaptura(captura('payload-01.json'), store)
    expect(r.tipo).toBe('busca')
    expect(r.novos).toBeGreaterThan(0)
    expect(store.total()).toBe(r.novos)
  })

  it('soma ao longo de várias capturas', () => {
    const store = new AdStore()
    const a = processarCaptura(captura('payload-01.json'), store)
    const b = processarCaptura(captura('payload-02.json'), store)
    expect(b.total).toBe(a.novos + b.novos)
    expect(store.total()).toBe(b.total)
  })

  it('a mesma captura duas vezes não indexa de novo', () => {
    const store = new AdStore()
    processarCaptura(captura('payload-01.json'), store)
    const segunda = processarCaptura(captura('payload-01.json'), store)
    expect(segunda.novos).toBe(0)
  })

  it('ignora captura que não é de anúncios', () => {
    const store = new AdStore()
    const r = processarCaptura(
      { url: 'https://www.facebook.com/ajax/bz', corpo: '{"a":1}' },
      store,
    )
    expect(r.tipo).toBe('ignorar')
    expect(r.novos).toBe(0)
  })

  it('corpo inválido não derruba nada', () => {
    const store = new AdStore()
    const r = processarCaptura(
      { url: 'https://www.facebook.com/api/graphql/', corpo: 'não é json' },
      store,
    )
    expect(r.tipo).toBe('ignorar')
    expect(r.novos).toBe(0)
  })

  it('a presença do anunciante é contada ao indexar', () => {
    const store = new AdStore()
    processarCaptura(captura('payload-01.json'), store)
    const algum = store.todos()[0]
    expect(store.presenca(algum.anunciante.pageId)).toBeGreaterThan(0)
  })
})
```

- [ ] **Step 2: Rodar e confirmar que falha**

```bash
npx.cmd vitest run tests/pipeline.test.ts
```

- [ ] **Step 3: Escrever a implementação**

Criar `src/content/pipeline.ts`:

```ts
import { normalizarBusca } from '../core/normalize'
import { classificar, type TipoCaptura } from '../core/router'
import type { AdStore } from '../core/store'
import type { Captura } from '../interceptor/xhr-patch'

/** A Meta prefixa respostas com isto para impedir sequestro de JSON. */
const PREFIXO_ANTI_SEQUESTRO = /^\s*for\s*\(\s*;\s*;\s*\)\s*;/

export interface ResultadoCaptura {
  tipo: TipoCaptura
  /** Quantos anúncios inéditos entraram no índice. */
  novos: number
  /** Tamanho do índice depois desta captura. */
  total: number
}

/**
 * O tubo: uma captura crua entra, anúncios indexados saem.
 *
 * É função pura sobre o store para poder ser testada contra as fixtures
 * reais, sem navegador. O content script fica sendo só a cola entre a
 * mensagem que chega e esta função.
 */
export function processarCaptura(
  captura: Captura,
  store: AdStore,
): ResultadoCaptura {
  const tipo = classificar(captura)
  if (tipo !== 'busca') {
    return { tipo, novos: 0, total: store.total() }
  }

  const antes = store.total()
  try {
    const corpo = JSON.parse(
      captura.corpo.replace(PREFIXO_ANTI_SEQUESTRO, ''),
    ) as unknown
    store.adicionar(normalizarBusca(corpo))
  } catch {
    // Uma resposta estranha não pode derrubar a sessão inteira.
    return { tipo: 'ignorar', novos: 0, total: antes }
  }

  const total = store.total()
  return { tipo, novos: total - antes, total }
}
```

- [ ] **Step 4: Rodar e confirmar que passa**

```bash
npx.cmd vitest run tests/pipeline.test.ts
```

Esperado: PASSA, 6 testes verdes.

---

## Task 2: Ligar no content script e provar na Biblioteca real

**Files:**
- Modify: `src/content/index.ts` (acrescentar, sem remover o que existe)
- Test: `e2e/pipeline.spec.ts`

**Interfaces:**
- Consumes: `processarCaptura` da Task 1; `AdStore`; o contrato de mensagens.
- Produces: linha de console `[CopyHaunt] indexados: <n>` a cada captura útil.

- [ ] **Step 1: Ligar o tubo no content script**

Em `src/content/index.ts`, acrescentar o import e trocar apenas o corpo do
listener de mensagens. O restante do arquivo — a montagem do painel — fica
como está.

Novo topo do arquivo:

```ts
import { isCopyHauntMessage } from '../core/messages'
import { AdStore } from '../core/store'
import type { Captura } from '../interceptor/xhr-patch'
import { processarCaptura } from './pipeline'
```

Acrescentar, logo abaixo dos imports:

```ts
/** Índice da sessão. Vive enquanto a aba viver. */
const store = new AdStore()
```

Substituir o listener existente por:

```ts
window.addEventListener('message', (event) => {
  if (event.source !== window) return
  if (!isCopyHauntMessage(event.data)) return

  if (event.data.kind === 'interceptor-ready') {
    console.info('[CopyHaunt] interceptador confirmado pelo content script')
    return
  }

  if (event.data.kind === 'raw-capture') {
    const resultado = processarCaptura(event.data.payload as Captura, store)
    if (resultado.novos > 0) {
      console.info(`[CopyHaunt] indexados: ${resultado.total}`)
    }
  }
})
```

- [ ] **Step 2: Escrever o teste de ponta a ponta**

Criar `e2e/pipeline.spec.ts`:

```ts
import { expect, test } from './fixtures'

const URL_BUSCA =
  'https://www.facebook.com/ads/library/?active_status=active&ad_type=all' +
  '&country=BR&q=receitas&search_type=keyword_unordered'

/**
 * Prova o caminho inteiro na Biblioteca real: a página pede, o interceptador
 * captura, o roteador classifica, o normalizador converte e o índice cresce.
 *
 * As peças têm teste cada uma; este é o único que exercita a ligação.
 */
test('o tubo indexa anúncios reais de ponta a ponta', async ({ context }) => {
  const page = await context.newPage()
  const linhas: string[] = []
  page.on('console', (m) => linhas.push(m.text()))

  await page.goto(URL_BUSCA, { waitUntil: 'domcontentloaded', timeout: 30_000 })
  for (let i = 0; i < 3; i += 1) {
    await page.mouse.wheel(0, 4000)
    await page.waitForTimeout(2500)
  }

  const indexacoes = linhas
    .filter((l) => l.includes('[CopyHaunt] indexados:'))
    .map((l) => Number(l.split(':').pop()?.trim() ?? 0))

  console.log('  indexações observadas:', indexacoes.join(' → '))

  // Houve indexação.
  expect(indexacoes.length).toBeGreaterThan(0)

  // O índice cresceu: mais de um punhado de anúncios reais entrou.
  const maior = Math.max(...indexacoes)
  expect(maior).toBeGreaterThan(5)

  // A contagem nunca diminui — é índice acumulado, não contador por lote.
  for (let i = 1; i < indexacoes.length; i += 1) {
    expect(indexacoes[i]).toBeGreaterThanOrEqual(indexacoes[i - 1])
  }
})
```

- [ ] **Step 3: Compilar e rodar o e2e**

```bash
npm.cmd run build
npx.cmd playwright test e2e/pipeline.spec.ts --timeout=120000
```

Esperado: PASSA, e a linha `indexações observadas:` mostra a contagem
crescendo.

**Se nenhuma indexação aparecer, PARE e reporte** as linhas de console
capturadas. Significa que a ligação não fechou, e saber onde ela parou é o
valor deste teste.

- [ ] **Step 4: Rodar tudo**

```bash
npm.cmd test
npm.cmd run typecheck
npm.cmd run verify:build
npx.cmd playwright test
```

- [ ] **Step 5: Escrever a mensagem de commit**

Criar `.commit-msg` na raiz com:

```
✨ feat(normalizer): ligar captura, classificação, normalização e índice

O que foi feito:
- Acrescentar o tubo que transforma captura crua em anúncios indexados
- Ligar o tubo ao content script e provar o caminho na Biblioteca real

Como foi feito:
- O tubo é função pura sobre o store, testada contra as fixtures reais sem
  navegador. O content script fica sendo só a cola entre a mensagem e ela
- O e2e mede a contagem crescendo em anúncios reais: é o único teste que
  exercita a ligação, já que cada peça tinha só teste de unidade

Considerações:
- Antes disto, todas as peças estavam prontas e nenhuma estava ligada. O
  content script importava uma única função. Unidades verdes, sistema inerte
```

---

## Verificação final

```bash
npm.cmd test
npm.cmd run typecheck
npm.cmd run verify:build
npx.cmd playwright test
git status --short
```
